import { buildSiteArchitecture, classifyLink, simulateSiteChange } from "../seo/siteArchitecture"; import { analyzeContent } from "../seo/contentIntelligence"; import { advanceCopilot, createCampaign, createCopilot, groundedRecommendation, keywordConflicts, normalizeKeyword, opportunityScore, prioritizeForCampaign } from "../seo/strategy";
const rec=(url:string,links:string[]=[])=>({url,normalizedUrl:url,status:200,depth:url.endsWith("/")?0:1,title:url,description:"d",canonical:url,indexable:true,blockedByRobots:false,links,issueCount:0,bytes:10});
describe("SEO 6 site architecture",()=>{it("builds graphs, ranks pages, classifies and simulates",()=>{const g=buildSiteArchitecture([rec("https://x/",["https://x/a"]),rec("https://x/a",[])],{}, {sitemap:["https://x/a"]});expect(g.graphs.internal).toHaveLength(1);expect(g.nodes.find(n=>n.url.endsWith("/a"))!.rank).toBeGreaterThan(0);expect(classifyLink({selector:"footer a"})).toBe("footer");expect(simulateSiteChange(g,{removeUrl:"https://x/a"}).blastRadius).toBeGreaterThan(0)})});
describe("SEO 7 content",()=>{it("finds thin, stale, duplicates and cannibalization without ranking myths",()=>{const d={title:"Shoes",h1:"Shoes",text:"same small text",intent:"commercial",queries:["shoes"],updatedAt:"2020-01-01"};const r=analyzeContent([{...d,url:"/a"},{...d,url:"/b"}]);expect(r.findings.some(x=>x.ruleId==="near-duplicate")).toBe(true);expect(r.findings.find(x=>x.ruleId==="thin-value")!.evidence).toContain("not a ranking-factor")})});
describe("SEO 8 keywords",()=>{it("normalizes, detects conflicts and explains score",()=>{const a=normalizeKeyword({term:" Shoes ",targetUrl:"/a"}),b=normalizeKeyword({term:"shoes",targetUrl:"/b",intent:"transactional"});expect(a.term).toBe("shoes");expect(keywordConflicts([a,b])).toContain("Competing URLs: shoes");expect(opportunityScore({businessValue:100,relevance:100,evidence:100,feasibility:100,effort:0}).score).toBe(100)})});
describe("SEO 9 campaigns",()=>{it("models templates, KPIs and relevance ranking",()=>{const c=createCampaign({name:"Launch",template:"launch",goal:"leads",start:"2026-01-01",end:"2026-02-01",audiences:[],markets:[],topics:[],targetUrls:[],competitors:[],owners:["a"],budget:10,dependencies:[],overrides:{crawl:{maxPages:5}},kpis:[{name:"clicks",baseline:1,target:2,milestones:[1.5]}],approvalRoles:["owner"]});expect(prioritizeForCampaign([{id:"a",campaignIds:[c.id],businessImpact:50,confidence:80,effort:10,risk:10},{id:"b",businessImpact:50,confidence:80,effort:10,risk:10}],c.id)[0].id).toBe("a")})});
describe("SEO 10 copilot",()=>{it("enforces grounding, workflow, approval and budgets",()=>{let s=createCopilot("p","c","operate",{tokens:10,cost:1,seconds:10,files:1,approvals:1});expect(()=>groundedRecommendation(s,"rank guaranteed")).toThrow();expect(groundedRecommendation(s,"proposal","audit:1").rankingGuarantee).toBe(false);s=advanceCopilot(s,"evidence");s=advanceCopilot(s,"preview");expect(()=>advanceCopilot(s,"approval")).toThrow(/approval/);s=advanceCopilot(s,"approval",{approvals:1});expect(()=>advanceCopilot(s,"apply",{files:2})).toThrow(/files/)})});
