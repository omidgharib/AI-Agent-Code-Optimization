import { Component, type ErrorInfo, type ReactNode } from "react";
export class ErrorBoundary extends Component<{ children: ReactNode }, { error?: Error }> { state: { error?: Error } = {}; static getDerivedStateFromError(error: Error) { return { error }; } componentDidCatch(error: Error, info: ErrorInfo) { console.error(error, info); } render() { const fa = localStorage.getItem("ai-auditor-lang") !== "en"; return this.state.error ? <main className="route-page"><h1>{fa ? "مشکلی پیش آمد" : "Something went wrong"}</h1><p role="alert">{this.state.error.message}</p><button onClick={() => location.reload()}>{fa ? "بارگذاری دوباره" : "Reload"}</button></main> : this.props.children; } }
