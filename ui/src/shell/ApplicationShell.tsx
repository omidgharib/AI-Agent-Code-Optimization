import { useEffect, useState, type ReactNode } from "react";
const links = [["/code", "Code Audit"], ["/seo", "SEO Workspace"], ["/runs/recent", "Runs"], ["/settings", "Settings"]] as const;
export function ApplicationShell({ children }: { children: ReactNode }) { const pathname = window.location.pathname; const [lang, setLang] = useState<"fa" | "en">(() => localStorage.getItem("ai-auditor-lang") === "en" ? "en" : "fa"); useEffect(() => { document.documentElement.lang = lang; document.documentElement.dir = lang === "fa" ? "rtl" : "ltr"; localStorage.setItem("ai-auditor-lang", lang); }, [lang]); const labels = lang === "fa" ? ["ممیزی کد", "فضای SEO", "اجراها", "تنظیمات"] : links.map((item) => item[1]); return <div className="global-shell"><header className="global-header"><a className="global-brand" href="/code"><span>AI</span><strong>AI Auditor</strong></a><div className="global-header-actions"><nav aria-label="Workspace">{links.map(([href], index) => <a key={href} className={pathname === href || (href.startsWith("/runs") && pathname.startsWith("/runs/")) ? "active" : ""} href={href}>{labels[index]}</a>)}</nav><button className="global-language" data-testid="language-toggle" type="button" onClick={() => setLang(lang === "fa" ? "en" : "fa")} aria-label={lang === "fa" ? "Switch to English" : "تغییر زبان به فارسی"}>{lang === "fa" ? "EN" : "فا"}</button></div></header><div className="global-content">{children}</div></div>; }
