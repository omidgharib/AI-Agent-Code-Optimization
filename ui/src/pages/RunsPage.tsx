import { useEffect, useState } from "react";
import { api } from "../api/client";
export function RunsPage() { const [jobs, setJobs] = useState<Array<{ id: string; status: string }>>([]); useEffect(() => { void api<Array<{ id: string; status: string }>>("/api/jobs").then(setJobs).catch(() => setJobs([])); }, []); return <main className="route-page"><header className="route-heading"><div><span className="eyebrow">ACTIVITY</span><h1>Runs</h1><p>Recent Code Audit and compatibility runs.</p></div></header><section className="route-card">{jobs.length ? <ul className="run-list">{jobs.map((job) => <li key={job.id}><code>{job.id}</code><span className={`status-pill ${job.status}`}>{job.status}</span></li>)}</ul> : <div className="empty-state"><strong>No runs yet</strong><p>Start an audit from the Code or SEO workspace.</p></div>}</section></main>; }
